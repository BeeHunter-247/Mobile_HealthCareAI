import Logo from "../components/ui/Logo";

function HomePageScreen() {
    return (
        <Logo icon="add"
        color="#cccccc"
        size={26}
        onPress={() => {}}></Logo>
    );
}

export default HomePageScreen;
